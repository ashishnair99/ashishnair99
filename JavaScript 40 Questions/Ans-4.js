// Write a program to check if a number is odd or even.

var num;
num = window.prompt("Enter a Number to check no. is Even or Odd:");

if (num % 2 == 0)
    console.log(num + " is an Even Number");
else
    console.log(num + " is an Odd Number");